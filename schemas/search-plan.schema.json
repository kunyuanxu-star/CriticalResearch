{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Search Plan Schema",
  "description": "Search strategy with query classes. Required by S06 generate_search_strategy.",
  "type": "object",
  "required": ["schema_version"],
  "properties": {
    "schema_version": { "type": "string", "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$" }
  }
}
