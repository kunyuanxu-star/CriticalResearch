{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Baseline Positioning Schema",
  "description": "Strongest baseline, novelty threat. Required by S15 synthesize_baseline_positioning.",
  "type": "object",
  "required": ["schema_version"],
  "properties": {
    "schema_version": { "type": "string", "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$" }
  }
}
