{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Evaluation Gap Map Schema",
  "description": "Per-claim evaluation contract and gaps. Required by S16 synthesize_evaluation_gaps.",
  "type": "object",
  "required": ["schema_version"],
  "properties": {
    "schema_version": { "type": "string", "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$" }
  }
}
