{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Phase Manifest Schema",
  "description": "Schema for workflow/phase-manifest.yaml — the single source of truth for phase definitions in a 37-phase paper round.",
  "type": "object",
  "required": ["schema_version", "phase_count", "close_round_is_phase", "execution_policy", "phases"],
  "properties": {
    "schema_version": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$"
    },
    "phase_count": {
      "type": "integer",
      "minimum": 37,
      "maximum": 37
    },
    "close_round_is_phase": {
      "type": "boolean",
      "enum": [true]
    },
    "execution_policy": {
      "type": "object",
      "required": ["full_round_required", "full_paper_required", "required_phase_count", "allow_partial_stop", "stop_only_on"],
      "properties": {
        "full_round_required": { "type": "boolean", "enum": [true] },
        "full_paper_required": { "type": "boolean", "enum": [true] },
        "required_phase_count": { "type": "integer", "minimum": 37, "maximum": 37 },
        "allow_partial_stop": { "type": "boolean", "enum": [false] },
        "stop_only_on": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "minLength": 1 }
        }
      }
    },
    "phases": {
      "type": "array",
      "minItems": 37,
      "maxItems": 37,
      "items": { "$ref": "#/definitions/phase" }
    }
  },
  "definitions": {
    "phase": {
      "type": "object",
      "required": [
        "order", "id", "module", "prompt", "dependencies",
        "required_inputs", "required_outputs", "allowed_write_scopes",
        "forbidden_actions", "objective_binding_required",
        "full_paper_coverage_required", "external_review_required",
        "state_required", "no_op_allowed"
      ],
      "properties": {
        "order": {
          "type": "integer",
          "minimum": 1,
          "maximum": 37
        },
        "id": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9_]*$"
        },
        "module": {
          "type": "string",
          "enum": ["M0", "M1", "M2", "M3", "M4", "M5", "M6", "M7"]
        },
        "prompt": {
          "type": "string",
          "pattern": "^prompts/phases/[a-z0-9_]+\\.md$"
        },
        "dependencies": {
          "type": "array",
          "items": { "type": "string", "pattern": "^[a-z][a-z0-9_]*$" }
        },
        "required_inputs": {
          "type": "array",
          "items": { "$ref": "#/definitions/artifact_ref" }
        },
        "required_outputs": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/definitions/output_ref" }
        },
        "allowed_write_scopes": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 }
        },
        "forbidden_actions": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 }
        },
        "objective_binding_required": { "type": "boolean" },
        "full_paper_coverage_required": { "type": "boolean" },
        "external_review_required": { "type": "boolean", "enum": [false] },
        "state_required": {
          "type": "string",
          "enum": ["running"]
        },
        "no_op_allowed": { "type": "boolean" },
        "coverage_exemption_reason_required": { "type": "boolean" }
      }
    },
    "artifact_ref": {
      "type": "object",
      "required": ["path", "type"],
      "properties": {
        "path": {
          "type": "string",
          "pattern": "^(project:|round:|workspace:)[^\\s]+$"
        },
        "type": {
          "type": "string",
          "enum": ["yaml", "markdown", "json", "directory"]
        }
      }
    },
    "output_ref": {
      "type": "object",
      "required": ["path", "type"],
      "properties": {
        "path": {
          "type": "string",
          "pattern": "^(project:|round:|workspace:)[^\\s]+$"
        },
        "type": {
          "type": "string",
          "enum": ["yaml", "markdown", "json", "directory"]
        },
        "schema": {
          "type": "string",
          "pattern": "^schemas/[a-z0-9_-]+\\.schema\\.json$"
        },
        "no_schema_reason": {
          "type": "string",
          "minLength": 10
        }
      }
    }
  }
}
