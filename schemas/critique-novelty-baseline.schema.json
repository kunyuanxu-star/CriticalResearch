{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Critique Novelty and Baseline Schema",
  "description": "Novelty and baseline coverage. Required by S18 critique_novelty_and_baselines.",
  "type": "object",
  "required": ["schema_version"],
  "properties": {
    "schema_version": { "type": "string", "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$" }
  }
}
